# Rendered outputs

Populated by `run_assessment.R`:

- `docs/assessment/<assessment_year>/` — copies of the three production renders.
- `docs/index/` — survey index exploration HTML.
- `docs/model_exploration_reports/` — benchmark/sensitivity reports from `src/exploration/`.

Everything under `docs/` is regenerable from `data/` and the qmd sources.
